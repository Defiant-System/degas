
// degas.sidebar

{
	init() {
		
	},
	dispatch(event) {
		let APP = degas,
			Self = APP.sidebar,
			el;
		// console.log(event);
		switch (event.type) {
			case "some-event":
				// TODO
				break;
		}
	}
}
