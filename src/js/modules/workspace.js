
// degas.workspace

{
	init() {
		
	},
	dispatch(event) {
		let APP = degas,
			Self = APP.workspace,
			el;
		// console.log(event);
		switch (event.type) {
			case "some-event":
				// TODO
				break;
		}
	}
}
