
// degas.colorpicker

{
	init() {

	},
	dispatch(event) {
		let APP = degas,
			Self = APP.colorpicker,
			el;
		// console.log(event);
		switch(event.type) {
			case "test-event":
				break;
		}
	}
}
