
// doob.sidebar.object

{
	init() {

	},
	dispatch(event) {
		let APP = doob,
			Self = APP.sidebar.object,
			el;
		// console.log(event);
		switch(event.type) {
			case "test-event":
				break;
		}
	}
}
