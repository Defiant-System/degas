
// degas.sidebar.object

{
	init() {

	},
	dispatch(event) {
		let APP = degas,
			Self = APP.sidebar.object,
			el;
		// console.log(event);
		switch(event.type) {
			case "test-event":
				break;
		}
	}
}
