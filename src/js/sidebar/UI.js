
// degas.sidebar.UI

{
	init() {
		
	},
	dispatch(event) {
		let APP = degas,
			Self = APP.sidebar.UI,
			name,
			value,
			pEl,
			el;
		// console.log(event);
		switch(event.type) {
			case "select-scene-bg":
				break;
		}
	}
}
