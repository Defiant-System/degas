
// doob.sidebar.texture

{
	init() {

	},
	dispatch(event) {
		let APP = doob,
			Self = APP.sidebar.texture,
			el;
		// console.log(event);
		switch(event.type) {
			case "test-event":
				break;
		}
	}
}
