
// degas.sidebar.files

{
	init() {

	},
	dispatch(event) {
		let APP = degas,
			Self = APP.sidebar.files,
			el;
		// console.log(event);
		switch(event.type) {
			case "test-event":
				break;
		}
	}
}
