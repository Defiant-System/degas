
// doob.sidebar.files

{
	init() {

	},
	dispatch(event) {
		let APP = doob,
			Self = APP.sidebar.files,
			el;
		// console.log(event);
		switch(event.type) {
			case "test-event":
				break;
		}
	}
}
