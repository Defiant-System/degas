
// degas.sidebar.material

{
	init() {

	},
	dispatch(event) {
		let APP = degas,
			Self = APP.sidebar.material,
			el;
		// console.log(event);
		switch(event.type) {
			case "test-event":
				break;
		}
	}
}
