
// degas.sidebar.settings

{
	init() {

	},
	dispatch(event) {
		let APP = degas,
			Self = APP.sidebar.settings,
			el;
		// console.log(event);
		switch(event.type) {
			case "test-event":
				break;
		}
	}
}
