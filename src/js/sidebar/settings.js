
// doob.sidebar.settings

{
	init() {

	},
	dispatch(event) {
		let APP = doob,
			Self = APP.sidebar.settings,
			el;
		// console.log(event);
		switch(event.type) {
			case "test-event":
				break;
		}
	}
}
