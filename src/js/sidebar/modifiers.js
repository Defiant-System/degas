
// degas.sidebar.modifiers

{
	init() {

	},
	dispatch(event) {
		let APP = degas,
			Self = APP.sidebar.modifiers,
			el;
		// console.log(event);
		switch(event.type) {
			case "test-event":
				break;
		}
	}
}
