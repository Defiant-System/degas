
// degas.sidebar.camera

{
	init() {

	},
	dispatch(event) {
		let APP = degas,
			Self = APP.sidebar.camera,
			el;
		// console.log(event);
		switch(event.type) {
			case "test-event":
				break;
		}
	}
}
